<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.k2data.k2app.quality.mapper.FileMapper" >

    <insert id="insert" parameterType="com.k2data.k2app.quality.domain.po.Project">
        insert into file (fileId,projectId,fileName,filePath,createTime)
        VALUES (#{fileId},#{projectId},#{fileName},#{filePath},#{createTime})
    </insert>

</mapper>